This folder contains some utilities file which can be used to run batch
    jobs(```run #.job```)

        It also stores the file ```python_hepmc
            .py``` which can used split large HEPMC files.
